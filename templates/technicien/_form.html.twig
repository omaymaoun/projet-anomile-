{# technicien/_form.html.twig #}

{{ form_start(form) }}

    {{ form_widget(form) }}

    <div class="mt-3">
        <button type="submit" class="btn btn-warning btn-custom btn-sm">
            <i class="bi bi-pencil-square me-1"></i>
            {{ button_label|default('Mettre à jour') }}
        </button>
    </div>

{{ form_end(form) }}
